package sample.kotlin.conditional

// NAO CONSIGO PENSAR EM NENHUM EXEMPLO LEGAL

/**
 * Pergunta feita no curso
 *
 * Should you always replace if statement with when ?
 * false
 *
 * Explicacao: When expression is especially for more than two branches
 *
 * In the case we need just two branches if-statement is preferable
 *
 * */

fun fn() {
    // since kotlin 1.3
    when(val p = 1) { }
}

fun main() {

}